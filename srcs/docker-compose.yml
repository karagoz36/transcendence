services:
  postgres:
    image: postgres
    container_name: postgres
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - app-network
    env_file:
      .env
  django:
    build: ./django
    container_name: django
    volumes:
      - ./django/app:/app/
    networks:
      - app-network
    env_file:
      .env
    ports:
      - 3434:80

networks:
  app-network:
    driver: bridge
